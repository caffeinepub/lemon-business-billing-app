{
  "frontend/src/App.tsx": "Main app with TanStack Router defining four routes, now wrapping the root layout with LanguageProvider for app-wide language context access.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend integration — customer CRUD, transaction operations, and lemon summary; `useAddCustomer` now explicitly checks actor readiness with `isFetching`, calls `refetchQueries` after invalidation for immediate UI update, and logs errors for debugging.",
  "frontend/src/hooks/useTransactionCalculations.ts": "Custom hook using useMemo that computes totalAmount and netCredit with decimal-aware floating-point arithmetic, rounding results to 2 decimal places to avoid precision issues.",
  "frontend/src/utils/formatBillText.ts": "WhatsApp bill text formatter using existing translation keys; formats all bigint monetary values with a `fmt()` helper (`toFixed(2)`) in the generated text string.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header that shows an ArrowLeft back button (navigates to `/`) on all non-root routes, plus a language picker; uses existing `appTitle`/`appSubtitle`/`back`/`footerBuiltWith`/`footerUsing` translation keys and `useRouterState` to detect the current path.",
  "frontend/src/components/SummaryBanner.tsx": "Three-card summary banner using existing translation keys (`customersLabel`, `lemonsLabel`, `creditDueLabel`); accepts `totalCreditDue` and `totalLemonsSold` as `bigint` and formats credit due with `toFixed(2)`.",
  "frontend/src/components/AddCustomerModal.tsx": "Add customer dialog modal with form submission that checks actor readiness before calling the mutation, disables inputs during pending state, uses plain Button (no DialogClose) for cancel to avoid premature dialog closure, and resets form state on both cancel and successful submission.",
  "frontend/src/components/CustomerListItem.tsx": "Clickable customer row using existing translation keys (`creditDue`, `clear`); accepts `balance` as `bigint` and formats it with `toFixed(2)`.",
  "frontend/src/components/CustomerDashboard.tsx": "Customer detail header card using existing translation keys (`creditDueCard`, `outstanding`, `allClear`, `since`); accepts `balance` as `bigint` and displays it with `toFixed(2)`.",
  "frontend/src/components/TransactionListItem.tsx": "Collapsible transaction row using existing translation keys; formats all monetary bigint values with a `fmt()` helper (`toFixed(2)`), and uses the correct `/transaction/$transactionId/bill` route for bill navigation.",
  "frontend/src/components/TransactionEntryForm.tsx": "Transaction entry form with all numeric inputs updated to `step=\"0.01\"` for decimal support; parses floats for live previews, rounds to nearest integer via `Math.round` when submitting to the bigint-typed backend, and displays totals with `toFixed(2)`.",
  "frontend/src/components/BillLayout.tsx": "Printable bill card using existing translation keys; formats all bigint monetary values (rate, total, previous credit, payment, net credit) with a `fmt()` helper that returns `toFixed(2)` strings.",
  "frontend/src/pages/HomePage.tsx": "Home page with dashboard, summary banner, and customer list; `handleModalOpenChange` now explicitly calls `refetchCustomers` and `refetchSummary` when the modal closes to ensure the UI reflects newly added customers immediately.",
  "frontend/src/pages/CustomerDetailPage.tsx": "Customer detail page with transaction history and delete confirmation; all strings translated via useLanguage hook.",
  "frontend/src/pages/BillSummaryView.tsx": "Bill summary page with print/WhatsApp actions; all strings translated and formatBillText now receives the current language for localized WhatsApp bill text.",
  "frontend/src/index.css": "Global styles with lemon-themed OKLCH color system (warm yellows, fresh greens), Inter/Nunito fonts, and print media query to hide UI chrome and optimize bill layout.",
  "frontend/tailwind.config.js": "Extended Tailwind config with lemon business custom colors (lemon-yellow, lemon-green, lemon-dark, lemon-bg), Nunito/Inter font families, and custom shadow/radius tokens.",
  "frontend/src/main.tsx": "App entry point that configures QueryClient with 60-second staleTime, 5-minute gcTime, single retry, and no window-focus refetching for improved performance, then mounts the app with all providers.",
  "frontend/src/pages/MySummaryPage.tsx": "Personal lemon business summary page using existing translation keys; formats all monetary bigint values with `toFixed(2)` for consistent decimal display.",
  "frontend/src/i18n/translations.ts": "Complete translations file with all user-facing strings for English, Hindi (Devanagari), Marathi (Devanagari), and Kannada (Kannada script), covering all pages, components, forms, modals, and bill text.",
  "frontend/src/contexts/LanguageContext.tsx": "React context providing current language state, a setLanguage function (with localStorage persistence), and a t() translation lookup function; exports LanguageProvider and useLanguage hook."
}