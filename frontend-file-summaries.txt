{
  "frontend/src/App.tsx": "Main app with TanStack Router defining four routes using React.lazy() for code-splitting on all page components, wrapped with Suspense and LoadingSpinner fallback, and LanguageProvider for app-wide language context.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; all monetary/quantity params now use `number` (float) instead of `bigint` for `useAddCustomer`, `useAddTransaction`, `usePayCreditDue`, and `useGetCustomerBalance`; `enqueueOperation` calls use the correct two-argument signature; exports `useGetCreditPaymentsForCustomer` alias.",
  "frontend/src/hooks/useTransactionCalculations.ts": "Custom hook using useMemo that computes `totalAmount` and `netCredit` with full floating-point arithmetic using `parseFloat`; accepts `previousCredit` as `number` and rounds results to 2 decimal places.",
  "frontend/src/utils/formatBillText.ts": "WhatsApp bill text formatter; `fmt()` helper updated to accept `number` instead of `bigint`, and `lemonQuantity` displayed with `.toFixed(2)`.",
  "frontend/src/components/Layout.tsx": "App shell updated to import useOfflineSync and render a sticky status banner above the header showing offline state (amber), syncing progress (green spinner), sync errors with retry, or pending count with sync-now button.",
  "frontend/src/components/SummaryBanner.tsx": "Three-card summary banner (customers, lemons sold, credit due) wrapped with React.memo; accepts `customerCount` and a `LemonSummary` object, using existing translation keys `customersLabel`, `lemonsLabel`, and `creditDueLabel`.",
  "frontend/src/components/AddCustomerModal.tsx": "Add Customer modal with `step=\"0.01\"` on the previous credit input; uses `parseFloat` and passes a `number` to the mutation; uses only valid existing translation keys.",
  "frontend/src/components/CustomerListItem.tsx": "Clickable customer row; `balance` prop changed from `bigint` to `number`, all `BigInt(0)` comparisons replaced with `> 0`, and `previousCredit` compared as plain number.",
  "frontend/src/components/CustomerDashboard.tsx": "Customer detail header card; `balance` prop changed from `bigint` to `number`, `hasCredit` uses plain `> 0` comparison, and balance displayed with `.toFixed(2)`.",
  "frontend/src/components/TransactionListItem.tsx": "Transaction row component with expandable details and delete confirmation; `fmt()` updated to accept `number`, all `BigInt` comparisons replaced with plain number comparisons, and `lemonQuantity` displayed via `fmt()`.",
  "frontend/src/components/TransactionEntryForm.tsx": "Transaction entry form with `step=\"0.01\"` on all numeric inputs; passes parsed float values directly to `addTransaction`; `currentBalance` prop typed as `number`; uses only valid existing translation keys.",
  "frontend/src/components/BillLayout.tsx": "Printable bill card using existing translation keys; `fmt()` helper updated to accept `number` instead of `bigint`, and all `BigInt(0)` comparisons replaced with plain `> 0`.",
  "frontend/src/pages/HomePage.tsx": "Home page with `balance` default value changed from `BigInt(0)` to `0` to match the updated `number` type from `useGetCustomerBalance`.",
  "frontend/src/pages/CustomerDetailPage.tsx": "Customer detail page with all balance/payment logic using `number` instead of `bigint`; payment input uses `step=\"0.01\"` and `parseFloat`; uses only valid existing translation keys; imports `useGetCreditPaymentsForCustomer` alias.",
  "frontend/src/pages/BillSummaryView.tsx": "Bill summary page that reads both `customerId` and `transactionId` from the new route path `/customer/$customerId/bill/$transactionId` via `useParams`, with print and WhatsApp share actions.",
  "frontend/src/index.css": "Global styles with lemon-themed OKLCH color system (warm yellows, fresh greens), Inter/Nunito fonts, and print media query to hide UI chrome and optimize bill layout.",
  "frontend/tailwind.config.js": "Extended Tailwind config with lemon business custom colors (lemon-yellow, lemon-green, lemon-dark, lemon-bg), Nunito/Inter font families, and custom shadow/radius tokens.",
  "frontend/src/main.tsx": "App entry point that configures QueryClient with 30-second staleTime, 5-minute gcTime, single retry, and no window-focus refetching for improved performance, then mounts the app with all providers.",
  "frontend/src/pages/MySummaryPage.tsx": "Personal lemon business summary page; all `BigInt(0)` defaults replaced with `0`, and all `Number()` wrappers removed since summary values are already `number` type.",
  "frontend/src/i18n/translations.ts": "Complete translations file for all four languages (en, hi, mr, kn) with all translation keys including new ones for previousCreditDue, payCreditDue, and CreditPaymentListItem; now exports the `TranslationKey` type derived from the English translations object keys to fix the TypeScript error in LanguageContext.",
  "frontend/src/contexts/LanguageContext.tsx": "React context providing current language state, a setLanguage function (with localStorage persistence), and a t() translation lookup function; exports LanguageProvider and useLanguage hook.",
  "frontend/src/components/CreditPaymentListItem.tsx": "Credit payment row with expandable details and delete confirmation; `fmt()` updated to accept `number` and `resultingCreditBalance > 0` comparison uses plain number comparison.",
  "frontend/src/components/LoadingSpinner.tsx": "Simple centered loading spinner component used as the Suspense fallback during lazy route chunk loading, styled with lemon theme colors.",
  "frontend/index.html": "HTML entry point updated with PWA manifest link, theme-color meta tag, Apple PWA meta tags, and inline service worker registration script.",
  "frontend/public/manifest.webmanifest": "PWA web app manifest for Lemon Billing App with name, icons (192x192 and 512x512), standalone display mode, lemon-yellow theme color, and start_url '/'.",
  "frontend/public/sw.js": "Service worker with cache-first strategy for static assets, navigation fallback to cached index.html when offline, skipWaiting+clients.claim for immediate activation, and a message handler for forced updates. Bumped to cache version v4 to force cache refresh on this production deployment.",
  "frontend/src/utils/offlineQueue.ts": "New IndexedDB-based offline queue utility that stores pending mutations (addCustomer, addTransaction, payCreditDue) with type, payload, timestamp, and synced flag; provides enqueueOperation, getPendingOperations, removeOperation, getPendingCount, and clearAllOperations functions.",
  "frontend/src/hooks/useOfflineSync.ts": "Offline sync hook that processes the IndexedDB queue on reconnect; updated to pass `number` (float) values directly to `addCustomer`, `addTransaction`, and `payCreditDue` instead of wrapping them in `BigInt()`.",
  "frontend/src/hooks/usePWAInstall.ts": "PWA install hook that captures beforeinstallprompt, detects iOS Safari, exposes canInstall/showBanner/showIOSInstructions flags, provides promptToInstall and dismiss methods with localStorage persistence, and detects standalone mode.",
  "frontend/src/components/InstallAppBanner.tsx": "Dismissible install banner that shows a native install button when the PWA prompt is available, or a \"How?\" button with a step-by-step iOS Safari dialog (Share → Add to Home Screen) for iOS users."
}