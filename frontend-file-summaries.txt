{
  "frontend/src/App.tsx": "Main app with TanStack Router defining four routes using React.lazy() for code-splitting on all page components, wrapped with Suspense and LoadingSpinner fallback, and LanguageProvider for app-wide language context.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; `useAddCustomer` (and other mutations) now check `actorFetching` inside the `mutationFn` and throw clear error messages instead of silently failing, while the submit button in the modal is no longer blocked by actor initialization state.",
  "frontend/src/hooks/useTransactionCalculations.ts": "Custom hook using useMemo that computes totalAmount and netCredit with decimal-aware floating-point arithmetic, rounding results to 2 decimal places to avoid precision issues.",
  "frontend/src/utils/formatBillText.ts": "WhatsApp bill text formatter using existing translation keys; formats all bigint monetary values with a `fmt()` helper (`toFixed(2)`) in the generated text string.",
  "frontend/src/components/Layout.tsx": "App shell updated to import useOfflineSync and render a sticky status banner above the header showing offline state (amber), syncing progress (green spinner), sync errors with retry, or pending count with sync-now button.",
  "frontend/src/components/SummaryBanner.tsx": "Three-card summary banner (customers, lemons sold, credit due) wrapped with React.memo; accepts `customerCount` and a `LemonSummary` object, using existing translation keys `customersLabel`, `lemonsLabel`, and `creditDueLabel`.",
  "frontend/src/components/AddCustomerModal.tsx": "Fixed Add Customer modal that removes the overly restrictive `canSubmit` gate — the submit button is now only disabled while the mutation is pending, with inline error messages shown if the actor isn't ready at submission time rather than blocking the button entirely.",
  "frontend/src/components/CustomerListItem.tsx": "Clickable customer row wrapped with React.memo to prevent unnecessary re-renders; displays customer info, balance, last transaction date, and an amber badge for previous credit due.",
  "frontend/src/components/CustomerDashboard.tsx": "Customer detail header card using existing translation keys (`creditDueCard`, `outstanding`, `allClear`, `since`); accepts `balance` as `bigint` and displays it with `toFixed(2)`.",
  "frontend/src/components/TransactionListItem.tsx": "Transaction row component fixed to pass { transactionId, customerId } object to deleteTransaction.mutateAsync matching the updated hook signature.",
  "frontend/src/components/TransactionEntryForm.tsx": "Transaction entry form that navigates to the new bill route `/customer/$customerId/bill/$transactionId` after a successful transaction submission, passing both IDs as path params.",
  "frontend/src/components/BillLayout.tsx": "Printable bill card using existing translation keys; formats all bigint monetary values (rate, total, previous credit, payment, net credit) with a `fmt()` helper that returns `toFixed(2)` strings.",
  "frontend/src/pages/HomePage.tsx": "Home page with auth gate, profile setup modal, prominent InstallAppBanner, compact Install button in the header area, search bar, summary banner, customer list with skeletons, and AddCustomerModal wired with onClose/onSuccess using only valid translation keys.",
  "frontend/src/pages/CustomerDetailPage.tsx": "Customer detail page with fixed credit payment validation (accepts decimals, enforces minimum ₹10, caps at outstanding balance, shows inline error messages) and wired-up delete credit payment functionality using the new `useDeleteCreditPayment` hook with proper error handling and toast notifications.",
  "frontend/src/pages/BillSummaryView.tsx": "Bill summary page that reads both `customerId` and `transactionId` from the new route path `/customer/$customerId/bill/$transactionId` via `useParams`, with print and WhatsApp share actions.",
  "frontend/src/index.css": "Global styles with lemon-themed OKLCH color system (warm yellows, fresh greens), Inter/Nunito fonts, and print media query to hide UI chrome and optimize bill layout.",
  "frontend/tailwind.config.js": "Extended Tailwind config with lemon business custom colors (lemon-yellow, lemon-green, lemon-dark, lemon-bg), Nunito/Inter font families, and custom shadow/radius tokens.",
  "frontend/src/main.tsx": "App entry point that configures QueryClient with 30-second staleTime, 5-minute gcTime, single retry, and no window-focus refetching for improved performance, then mounts the app with all providers.",
  "frontend/src/pages/MySummaryPage.tsx": "Personal lemon business summary page using existing translation keys; formats all monetary bigint values with `toFixed(2)` for consistent decimal display.",
  "frontend/src/i18n/translations.ts": "Complete translations file for all four languages (en, hi, mr, kn) with all translation keys including new ones for previousCreditDue, payCreditDue, and CreditPaymentListItem; now exports the `TranslationKey` type derived from the English translations object keys to fix the TypeScript error in LanguageContext.",
  "frontend/src/contexts/LanguageContext.tsx": "React context providing current language state, a setLanguage function (with localStorage persistence), and a t() translation lookup function; exports LanguageProvider and useLanguage hook.",
  "frontend/src/components/CreditPaymentListItem.tsx": "New component displaying a single credit due payment entry with date, amount paid, resulting balance, an expandable detail view, and a delete button with confirmation dialog (delete is a placeholder pending backend support).",
  "frontend/src/components/LoadingSpinner.tsx": "Simple centered loading spinner component used as the Suspense fallback during lazy route chunk loading, styled with lemon theme colors.",
  "frontend/index.html": "HTML entry point updated with PWA manifest link, theme-color meta tag, Apple PWA meta tags, and inline service worker registration script.",
  "frontend/public/manifest.webmanifest": "PWA web app manifest for Lemon Billing App with name, icons (192x192 and 512x512), standalone display mode, lemon-yellow theme color, and start_url '/'.",
  "frontend/public/sw.js": "Rewritten service worker with cache-first strategy for static assets (JS/CSS/images), navigation fallback to cached index.html when offline, skipWaiting+clients.claim for immediate activation, and a message handler for forced updates. Uses stale-while-revalidate for static assets to keep cache fresh.",
  "frontend/src/utils/offlineQueue.ts": "New IndexedDB-based offline queue utility that stores pending mutations (addCustomer, addTransaction, payCreditDue) with type, payload, timestamp, and synced flag; provides enqueueOperation, getPendingOperations, removeOperation, getPendingCount, and clearAllOperations functions.",
  "frontend/src/hooks/useOfflineSync.ts": "New custom hook that monitors online/offline status via window events, processes the IndexedDB offline queue when back online and authenticated, invalidates relevant React Query caches after sync, and returns isOnline, syncStatus, pendingCount, and syncPending for UI feedback.",
  "frontend/src/hooks/usePWAInstall.ts": "PWA install hook that captures beforeinstallprompt, detects iOS Safari, exposes canInstall/showBanner/showIOSInstructions flags, provides promptToInstall and dismiss methods with localStorage persistence, and detects standalone mode.",
  "frontend/src/components/InstallAppBanner.tsx": "Dismissible install banner that shows a native install button when the PWA prompt is available, or a \"How?\" button with a step-by-step iOS Safari dialog (Share → Add to Home Screen) for iOS users."
}